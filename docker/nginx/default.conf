# HTTP → HTTPS リダイレクト（本番）
server {
  listen 80;
  server_name links.hayasaka.co.jp;
  return 301 https://$host$request_uri;
}

# HTTP → HTTPS リダイレクト（ステージング）
server {
  listen 80;
  server_name staging.links.hayasaka.co.jp;
  return 301 https://$host$request_uri;
}

# HTTPS
server {
  listen 443 ssl;
  server_name links.hayasaka.co.jp;

  ssl_certificate     /etc/letsencrypt/live/links.hayasaka.co.jp/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/links.hayasaka.co.jp/privkey.pem;

  ssl_protocols       TLSv1.2 TLSv1.3;
  ssl_ciphers         HIGH:!aNULL:!MD5;

  client_max_body_size 50M;

  # API -> Django
  location /api/ {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  # メディアファイル（アップロード画像など） → Django
  location /media/ {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  # Django admin
  location /admin/ {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  # ログイン画面のみ Basic 認証
  location /login {
    auth_basic "Restricted";
    auth_basic_user_file /etc/nginx/.htpasswd;

    proxy_pass http://frontend:3000/login;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
  }

  # それ以外 -> Next.js（Basic認証なし、アプリ側で認証）
  location / {
    proxy_pass http://frontend:3000/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
  }
}

# ━━━━━━━━ ステージング ━━━━━━━━
server {
  listen 443 ssl;
  server_name staging.links.hayasaka.co.jp;

  ssl_certificate     /etc/letsencrypt/live/staging.links.hayasaka.co.jp/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/staging.links.hayasaka.co.jp/privkey.pem;

  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers   HIGH:!aNULL:!MD5;

  client_max_body_size 50M;

  location /api/ {
    proxy_pass http://172.17.0.1:8001;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  location /media/ {
    proxy_pass http://172.17.0.1:8001;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  location /admin/ {
    proxy_pass http://172.17.0.1:8001;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto https;
  }

  location / {
    proxy_pass http://172.17.0.1:3001/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto https;
  }
}
